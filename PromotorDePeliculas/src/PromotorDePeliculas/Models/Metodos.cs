using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Threading.Tasks;

namespace PromotorDePeliculas.Models
{
    /*
    public partial class Premio : IValidatableObject
      {
          public IEnumerable<ValidationResult> Validate(ValidationContext validationContext)
          {
              var prop = new[] { "Fecha" };
            if (fechaEntrega < Pelicula.FechaEstreno)
                 yield return new ValidationResult("Fecha inferior a la fecha de estreno", prop);
                
          }
      }
    
    public partial class Comentario : IValidatableObject
    {
        public IEnumerable<ValidationResult> Validate(ValidationContext validationContext)
        {
            var prop = new[] { "Fecha" };
            if (FechaComentario < Pelicula.FechaEstreno)
                yield return new ValidationResult("Fecha inferior a la fecha actual", prop);
            if (FechaComentario > DateTime.Now)
                yield return new ValidationResult("Fecha mayor a la fecha actual", prop);
        }
   }
 */






}
